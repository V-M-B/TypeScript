// to annotate return statement as well we use mul(x:number)":number" outside as well 
function mul(x:number) {
    return x*x
}
const output=mul(9)
console.log(output);



// Arrow function 
const sub=(y:number):number=> y-y

console.log(sub(99));
